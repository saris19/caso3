{
  "name": "caso3-frecuencia-tp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "sim:stats": "node --loader ts-node/esm --experimental-specifier-resolution=node scripts/sim-stats.ts 2>/dev/null || node dist/sim-stats.js"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.1"
  }
}
